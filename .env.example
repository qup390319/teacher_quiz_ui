# ==============================================
# 環境變數範本檔
# 使用方法：把這個檔案複製一份，命名為 .env，再填入實際的值
# 注意：.env 裡面有機密資訊，千萬不要 commit 到 GitHub！
# ==============================================

# -------------------
# 前端相關
# -------------------

# 前端對外的完整網址（等拿到 VM 的 IP 或網域後填入）
# 本地開發預設值：http://localhost:3000
# 部署到 VM 後範例：http://140.xxx.xxx.xxx:3000 或 https://sci-diang.hsueh.tw
FRONTEND_URL=http://localhost:3000

# -------------------
# 後端相關（目前畫面雛形階段不需要，未來對接後端時再填）
# -------------------

# 後端 API 的完整網址（在程式碼裡用 import.meta.env.VITE_BACKEND_URL 讀取）
# 注意：VITE_ 開頭的變數才會在 Vite 打包時注入到前端程式碼
# 本地開發預設值：http://localhost:8000
# 部署到 VM 後範例：http://140.xxx.xxx.xxx:8000 或 https://api.sci-diang.hsueh.tw
VITE_BACKEND_URL=http://localhost:8000
